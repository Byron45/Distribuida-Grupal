apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-web
  namespace: books-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-web
  template:
    metadata:
      labels:
        app: app-web
    spec:
      containers:
        - name: app-web
          image: byronsda45/app-web:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: app-web
  namespace: books-app
spec:
  type: NodePort
  selector:
    app: app-web
  ports:
    - port: 80
      targetPort: 80